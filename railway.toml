[build]
builder = "nixpacks"

[deploy]
startCommand = "python manage.py migrate && python manage.py createadmin && python manage.py collectstatic --noinput && gunicorn promptbuilder.wsgi:application"
healthcheckPath = "/"
healthcheckTimeout = 300